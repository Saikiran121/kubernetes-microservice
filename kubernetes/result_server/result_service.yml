apiVersion: v1
kind: Service
metadata:
  name: result-service
  labels:
    name: result-service
spec:
  type: NodePort
  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 30002
  selector:
    name: result-app-pod
