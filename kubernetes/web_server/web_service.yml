apiVersion: v1
kind: Service
metadata:
  name: phonebook-service
  labels:
    name: phonebook-service
spec:
  type: NodePort
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30001
  selector:
    name: phonebook-app-pod
